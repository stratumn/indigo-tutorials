version: '3'
services:
  agent:
    build:
      dockerfile: Dockerfile.test
    env_file:
    - ./config/test.env
    - ./config/test.secret.env
  store:
    env_file:
    - ./config/test.env
    - ./config/test.secret.env
  tmnode:
    user: root
    env_file:
    - ./config/test.env
    - ./config/test.secret.env
    volumes:
    - ./tendermint.test:/data/tendermint
networks:
  default:
    driver: bridge
    driver_opts:
        name: part4_test
